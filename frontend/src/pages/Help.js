import React, { useState } from "react";

export default () => (
  <div>
    <p>
      Need help with navigating the site? Have a bug that needs to be reported?
      Look no further!
    </p>
  </div>
);
